package xsd_loader

type (
	Schema struct {
		Elements        []*Element
		Types           []*Type
		Attributes      []*Attribute
		AttributeGroups []*AttributeGroup
	}

	Element struct {
	}

	Type struct {
	}

	Attribute struct {
	}

	AttributeGroup struct {
	}
)